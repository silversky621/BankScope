spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/bank?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=YOUR_DB_PASSWORD

mybatis.mapper-locations=classpath:/mappers/**/*.xml

mybatis.type-aliases-package=com.bankscope.backend.entities

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=YOUR_GMAIL_ADDRESS
spring.mail.password=YOUR_GMAIL_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

mybatis.configuration.default-enum-type-handler=org.apache.ibatis.type.EnumTypeHandler

server.forward-headers-strategy=framework

solapi.api.key=YOUR_SOLAPI_KEY
solapi.api.secret=YOUR_SOLAPI_SECRET
solapi.from.number=YOUR_PHONE_NUMBER

spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.session.store-type=redis
spring.session.redis.flush-mode=on_save
spring.session.timeout=2h

# 주민번호 보호 키. AES-GCM 키(정확히 16/24/32자) + 블라인드 인덱스용 HMAC 키.
# ⚠️ V4 마이그레이션이 '아래 값'으로 시드 주민번호를 재암호화/인덱싱하므로,
#    번들된 시드 데이터를 쓰려면 모든 환경에서 반드시 아래 값을 그대로 사용해야 한다(값을 바꾸면 키오스크 로그인/주민번호 복호화가 깨짐).
# (실서비스라면 키를 깃에 두지 않고 시크릿 매니저로 주입 + 환경별 키로 런타임 재암호화 마이그레이션을 쓰는 것이 맞다. 본 제출물은 더미 데이터 기준의 타협안.)
app.aes.secret-key=wCBlSoQjWaaDe68I
app.hmac.secret-key=qNc4AAgPIjU6joeCgrsBh0c2cBxKNkVq

# Flyway DB 마이그레이션: 앱 기동 시 classpath:db/migration 의 V1(스키마)·V2(시드)를 순서대로 적용·추적
# 빈 DB면 전체 적용, 이미 데이터가 있는 DB면 baseline 처리하여 재실행하지 않음
spring.flyway.enabled=true
spring.flyway.schemas=bank
spring.flyway.create-schemas=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=2
